# ========================================
# 🔥 ОСНОВНОЙ СТЕК: FastAPI, сервер, HTTP
# ========================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
python-multipart==0.0.6
websockets==12.0

# ========================================
# 🛢️ БАЗА ДАННЫХ: PostgreSQL, ORM, миграции
# ========================================
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.12.1

# ========================================
# 🔐 АВТОРИЗАЦИЯ: JWT, хэширование паролей
# ========================================
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ========================================
# 🧠 ИСКУССТВЕННЫЙ ИНТЕЛЛЕКТ: ML, обработка сигналов
# ========================================
torch==2.1.0
torchvision==0.16.0
neurokit2==0.2.7        # Анализ ЭКГ
opencv-python==4.8.1.78  # Обработка изображений (X-ray, MRI)
Pillow==10.1.0          # Работа с изображениями
numpy==1.24.3
pandas==2.1.3
pydicom==2.4.3          # Чтение DICOM (медицинские снимки)

# ========================================
# 🗣️ ГОЛОСОВОЙ ИНТЕРФЕЙС: речь → текст, текст → речь
# ========================================
SpeechRecognition==3.10.0  # Распознавание голоса
gTTS==2.4.0               # Текст в речь (Google Text-to-Speech)
pydub==0.25.1             # Работа с аудио (нужен для gTTS и обработки)
pygame==2.5.2             # Воспроизведение аудио (можно заменить на simpleaudio)

# ========================================
# 🌐 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ И КОНФИГУРАЦИЯ
# ========================================
python-dotenv==1.0.0
pydantic-settings==2.1.0

# ========================================
# 📅 ВСПОМОГАТЕЛЬНЫЕ УТИЛИТЫ
# ========================================
python-dateutil==2.8.2
Jinja2==3.1.2             # Шаблоны (если нужны email или PDF)